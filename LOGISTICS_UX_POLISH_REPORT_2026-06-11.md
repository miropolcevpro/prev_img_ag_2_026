# Logistics UX Polish Report — 2026-06-11

## Что исправлено

- Убран проблемный HTML `<select>`, который на Tilda мог открываться и сразу закрываться из-за глобальных событий/MutationObserver.
- Варианты транспорта теперь открываются отдельной кнопкой "Выбрать другой транспорт".
- Список вариантов по умолчанию скрыт.
- Варианты отображаются вертикальными карточками без горизонтального скролла.
- Клик по варианту стабильно выбирает транспорт, сохраняет выбор в sessionStorage и закрывает список.
- MutationObserver теперь игнорирует собственный блок логистики, чтобы не перерисовывать меню в момент клика.
- Глобальный click listener больше не ломает кнопки внутри логистики.
- Основной расчет, цены, скидки, корзина и Tilda submit не менялись.

## Проверки

- `node --check paver-logistics-addon-final.js`: ok
- `logistics_rules.json`: valid JSON
- Внешний addon-файл оставлен с тем же именем для простой замены в GitHub.

## Версия

`logistics-addon-uxpolish-20260611-1`
